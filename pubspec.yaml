name: movie_flutter_app
description: A Flutter app for movie streaming
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5
  shared_preferences: ^2.0.15

flutter:
  uses-material-design: true
